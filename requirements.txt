# Requirements for the vLLM Explained Lab (CPU-only environment)
# Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

torch
transformers

# Quantization library for LLMs.
llmcompressor

# vLLM must be the CPU build — the generic PyPI wheel is a CUDA build and
# fails on GPU-less machines ("Device string must not be empty" at engine init).
# The wheel is Linux x86_64 only, so it's skipped on macOS; there is no
# published macOS wheel for vLLM (it requires a source build on Apple silicon).
vllm @ https://github.com/vllm-project/vllm/releases/download/v0.24.0/vllm-0.24.0+cpu-cp38-abi3-manylinux_2_34_x86_64.whl ; sys_platform == 'linux' and platform_machine == 'x86_64'

gradio
aiohttp
requests
